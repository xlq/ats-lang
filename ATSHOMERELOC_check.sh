#!/bin/bash

#if [ "${ATSHOMERELOC}" != "/usr/share/atshome" ] ; then
#  echo "The value of ATSHOMERELOC is set to \"${ATSHOMERELOC}\", but it should be set to \"/usr/share/atshome\"!"; exit 1;
#fi

echo "The value of ATSHOMERELOC is set to \"${ATSHOMERELOC}\"".

### end of [ATSHOMERELOC_check.sh] ###
